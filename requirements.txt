# Core ML Libraries
# Pin numpy <2.0 due to TF 2.20 compatibility window (avoids accidental numpy 2.x upgrade)
tensorflow>=2.8.0
keras>=2.8.0
numpy>=1.26.0,<2.0
pandas>=1.3.0
scikit-learn>=1.0.0
matplotlib>=3.5.0
seaborn>=0.11.0

# Federated Learning
flwr>=1.0.0

# Data Processing
scipy>=1.7.0
joblib>=1.1.0

# Visualization and Analysis
plotly>=5.0.0
ipywidgets>=7.6.0

# Development Tools
jupyter>=1.0.0
pytest>=6.2.0
black>=21.0.0
pylint>=2.12.0

# Network and Communication
grpcio>=1.43.0
protobuf>=3.19.0

# Security and Privacy
cryptography>=3.4.0

# Containerization / Orchestration (handled outside Python env)
# NOTE: Removed docker-compose Python package because it pins PyYAML<6 creating a conflict with security-updated PyYAML>=6.
# Use the Docker Compose V2 plugin (comes with recent Docker Desktop) or install separately, e.g.:
#   (Windows) Docker Desktop includes 'docker compose'
#   (Linux)   sudo apt-get install docker-compose-plugin
# If you REALLY need the legacy Python package, comment out PyYAML>=6 and add:
#   docker-compose==1.29.2
# but this is discouraged.

# Additional utilities
tqdm>=4.62.0
pyyaml>=6.0.0  # Security-updated; keep >=6 now that docker-compose (Python) removed
python-dotenv>=0.19.0
